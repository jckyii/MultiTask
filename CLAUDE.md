# CLAUDE.md — Multitask (native app)

This file gives Claude Code context at the start of every session. Keep it updated as the project progresses.

## What this project is
**Multitask Manager** (developer's pick, 2026-07-12 — app.json `name`; the slug stays `multitask` for EAS/Expo Go linkage) — a cross-platform task manager. A web version already exists and is deployed; this repo is the new **native app** (React Native + Expo) that will share data with the web app via Supabase. The web app will eventually be **redesigned to match** the improved native design, so what you build here is the reference design for the whole product, not just the mobile client.

- **Framework:** React Native via **Expo** (managed workflow / CNG). Currently pinned to **Expo SDK 54** (to match the installed Expo Go app during development).
- **Backend:** **direct-to-Supabase** with Row Level Security (no separate server in the data path). Supabase Auth handles login/tokens/verification.
- **Offline:** must be **offline-first** — local SQLite, syncs to Supabase when a connection returns (sync engine TBD: PowerSync vs WatermelonDB).
- **Platforms:** iOS + Android (primary), then desktop, then a re-skinned web. Developer is on **Windows** (iOS builds via EAS cloud; real iPhone used instead of the Mac-only simulator; iOS widgets will need occasional Mac access).

## Current state (update this as you go)
- **Project recreated on Expo SDK 54** (expo-router default template, at repo root) to match the Expo Go app on the developer's iPhone. The accidental SDK 57 root project and the nested duplicate `multitask/` folder were removed (2026-07-09). **Do not update Expo Go on the phone** — it supports one SDK at a time.
- Docs, design system, and web-app reference committed. `reference/config-and-infra/_env` contains live DB credentials — gitignored, must never be committed; rotating that DB password is recommended (coordinate with the Render env var).
- **Stage 1 (RLS) in progress.** SQL lives in `supabase/` (numbered files the developer runs in the Supabase SQL editor):
  - `00` (schema inspection) and `01` (enable RLS on `task`, `app_users`, `verification_tokens`) — **already run**; web app verified still working. The postgres role owns the tables so RLS doesn't apply to it — never use FORCE ROW LEVEL SECURITY.
  - `02` (adds `task.user_uuid` → auth.users, column defaults, nullable `user_id`, grants + 4 per-user policies, email-matching cross-fill trigger) — **run, verified (4 policies)**. Data API enabled.
  - `03` (re-runnable backfill linking old tasks to Supabase Auth signups by email) — run after signups.
- **Key schema facts (inspected live):** table is `task` (singular); `task_id` comes from `task_seq` (increment 50, Hibernate block allocator — a DB-default `nextval` coexists collision-free); `due_date` is wall-clock `timestamp without time zone`; 12 users / 19 tasks in prod. Supabase Data API currently disabled — enable it only after `02` is run.
- **Decisions made:** no formal account migration — everyone (incl. the developer) signs up fresh in Supabase Auth; old tasks auto-link by email via `03` + the insert trigger. The web app keeps Java auth untouched for the whole native build.
- **Supabase client + auth flow built (2026-07-09):** `lib/supabase.ts` (AsyncStorage session persistence, AppState token refresh), `hooks/use-auth.tsx` (AuthProvider/useAuth), bare-bones `app/sign-in.tsx` + `app/sign-up.tsx` (functional placeholders — real design pass comes later), `Stack.Protected` guards in `app/_layout.tsx`, temporary sign-out harness on the home tab. Config in `.env` (gitignored; template in `.env.example`). **Verified on-phone 2026-07-09:** sign-up, sign-in, session persistence across restarts, sign-out, guard navigation. Developer's account created; `supabase/03` run — developer's old tasks linked (16 tasks belonging to other web users remain unlinked until/unless they sign up).
- **Decision (2026-07-09): Supabase Auth "Confirm email" is OFF during the dev phase.** Confirm links redirect to the Site URL (localhost) and can't open the app under Expo Go (no custom link scheme there). Re-enable it — with `emailRedirectTo` + deep-link handling into the app — when the project moves to a development build (Stage 3 requires one anyway). Until then, signups take effect immediately.
- **Task data layer built (2026-07-09), online-only:** `lib/tasks/` — `types.ts` (Task model + row mappers, DB defaults mirrored), `dates.ts` (wall-clock parse/format — manual component parsing, NEVER toISOString for due dates), `status.ts` (deriveStatus ported from Task.java, strict-before semantics, default threshold 48h), `use-tasks.ts` (TanStack Query hooks: useTasks/useCreateTask/useSetTaskCompleted/useDeleteTask — invalidate-based for now, optimistic updates come with the real UI). 14 unit tests (`npm test`, jest-expo). Debug screen on the second tab (`app/(tabs)/explore.tsx`) — throwaway, exercises full CRUD. **Pending: on-phone verification of the debug screen.**
- **Open item for later:** urgency threshold is per-user in the web app (`app_users.urgent_threshold_hours`) but native users have no `app_users` row — native needs its own settings storage eventually (candidate: a `user_settings` table keyed by auth uuid, when Settings gets built). Using the 48h default constant until then.
- **Decision (2026-07-09): designed UI before offline sync** (matches roadmap Stage 2 order; stays in Expo Go's fast loop). Offline sync engine (PowerSync vs WatermelonDB) still open, revisit after core screens exist.
- **UI slice 1 built (2026-07-09): tokens + themed task list.** `lib/theme/` (tokens.ts transcribing docs/design/03 — spacing/radius/type/motion/light+dark palettes; use-theme.ts hook — becomes a provider when user-selectable themes land; pill-colors.ts HSL contrast auto-adjust for pastel user colors). `components/pill.tsx`, `components/task-card.tsx` (full 02-components spec: accent bar, status surfaces, mono due date, overdue icon, pills, 32px complete button). `lib/tasks/sections.ts` + tests (Overdue/Today/Tomorrow/Later/No due date/Completed grouping). Home tab = real task list (SectionList, sticky headers, skeleton loading, factual empty state). JetBrains Mono loaded via expo-font. Auth harness moved to the Debug tab. 18 tests total. **Pending: on-phone review in light AND dark mode.**
- **Accent color is still the placeholder desaturated indigo** — developer picks the final anchor color when they see the build (swap once in lib/theme/tokens.ts + docs/design/03).
- **UI slice 2 built (2026-07-09): swipe gestures + optimistic everything.** Developer feedback applied after slice-1 review: Mail-style swipe (right = complete/restore with green/accent trail, left = delete with red trail; 40% threshold, haptic ticks, 240ms slide-off, LayoutAnimation gap-close) in `components/swipeable-task-card.tsx`; **no visible complete button** (screen readers get accessibilityActions instead); description removed from cards; priority badge first in the pill row; Completed section at TOP, collapsed by default, persisted (`hooks/use-completed-collapsed.ts`); "Upcoming" wording; all mutations optimistic with rollback (`use-tasks.ts`), undo toasts (`components/undo-toast.tsx`, 5s, undo-delete re-inserts via `toRestoreRow`). GestureHandlerRootView + UndoToastProvider wired in `app/_layout.tsx`. Decisions recorded in docs/design/02 + 03. Dark mode + slice 1 approved by developer. **Pending: on-phone review of the swipe feel.**
- **UI slice 3 built (2026-07-10): trash, regroup animations, calmer swipes.** Soft delete via `deleted_at` (**requires running `supabase/04-soft-delete.sql`** — pending on developer); "Deleted" section at bottom, collapsed + persisted, swipe right = restore / left = permanent delete; undo-delete flips `deleted_at` back (same row — `toRestoreRow`/re-insert removed); shared `lib/animate-layout.ts` LayoutAnimation makes tasks glide between groups on every regroup; refresh spinner only on physical pull; swipe threshold 30%; `hooks/use-collapsed-section.ts` generalizes the persisted collapse. Known tradeoff (accepted): web app shows soft-deleted tasks until web redesign. **04 run + verified on phone (2026-07-10).** Swipe/regroup motion hand-tuned with the developer and FINALIZED — values recorded in docs/design/05-motion.md (16.18% threshold; entrance = 60% travel, 380ms ease-out, no bounce; settle spring 26/240; error toasts on failed mutations; screen-driven entrance marks).
- **UI slice 4 built + approved (2026-07-10): quick-add.** `components/fab.tsx` + `app/quick-add.tsx` — a **transparentModal route, NOT an RN `<Modal>`** (CRITICAL lesson: RN Modal hosts content in a separate native window where Reanimated AND LayoutAnimation silently no-op on the new arch; three animation attempts died there before rehosting as a route fixed everything). Autofocus title; date chip → inline calendar, time chip → 15-min wheel (both slide open/closed via animated height, 220ms); keyboard **Done only dismisses the keyboard** — adding happens ONLY via the "Add task" button; sheet surface stays anchored to the screen bottom and pads by live keyboard height (no backdrop gap under the box; KeyboardAvoidingView removed); sheet slides up on open and down in sync with the keyboard on close; creation is optimistic (temp negative id via `lib/enter-marks.ts` module, swapped for the server row on success). Default due = next quarter-hour today. "+ details" fields deferred to the task-detail slice. Entrance-mark bookkeeping lives in `lib/enter-marks.ts` (shared by list + route). **Regroup entrance final: 647.2ms ease-out** (golden ratio pick; 05-motion updated).
- **UI slice 5 built (2026-07-10): the Daily view** (named "Daily", not "Today" — developer pick; "day view" reserved for the calendar drill-down). **Requires running `supabase/05-recurring-tasks.sql`** (recurring_task + recurring_completion tables, RLS, grants — "done today" = completion row for the local date, daily reset is derived, no cron). `lib/tasks/use-recurring.ts` (optimistic add/toggle/archive/unarchive), `hooks/use-task-actions.ts` (swipe handlers extracted from the list screen, shared by both tabs), `app/(tabs)/daily.tsx` (h1 + date, Recurring pill-rows with check circles — tap toggles, long-press archives with undo, inline add link — and a Due-today section of swipeable cards incl. overdue). Tab bar: Tasks / Daily / Debug. **Pending: developer runs 05 + on-phone review.**
- **Quick-add v2 (2026-07-10, developer feedback):** default due time = **today 11:59 PM** (time wheel now minute-precise — 15-min steps can't express :59; recorded in docs/design/04); collapsed **"Details"** section (measured-height Collapsible) with priority chips + existing categories/subjects as selectable chips (data-driven from real tasks, colors shown) + description input; sheet body is a ScrollView (bounces off, max-height aware of keyboard), Add button pinned; dismissal remains tap-outside/Add only. Creating NEW categories/subjects deferred to the task-detail slice. Daily tab: add-recurring affordance is now an always-visible dashed ghost pill row (was a text link that hid on query error).
- **UI slice 6 built (2026-07-10): task edit, "+ New" pill creation, recurring swipes.** `components/task-form-sheet.tsx` is the shared form (quick-add + edit): date/time chips (with "Add date"/clear — tasks can have no due date), Details section, and **"＋ New" creators for categories/subjects** (name + 12-swatch color palette; new options become chips immediately and turn "existing" once saved on a task). `app/task/[id].tsx` = tap-any-card edit route (transparentModal like quick-add) with optimistic full-field `useUpdateTask`; long-press stays reserved for a future context menu. `components/swipeable-row.tsx` = the extracted generic swipe engine (all FINAL motion values live there now); `swipeable-task-card.tsx` is a thin semantic wrapper. Daily recurring rows now swipe with the same physics: right = check off → row slides off and re-enters a **"Done"** group below (green trail/check; in Done: accent trail/undo to un-check), left = remove (archive) with undo toast (replaced the old long-press Alert). Enter-marks are namespaced (`rec:${id}`) so recurring and task ids can't collide.
- **UI slice 7 built (2026-07-10): bulk clear.** "Clear all" in the Completed section header — batch soft-delete into the trash with ONE undo toast for the whole batch (entrance marks per task), section never collapses mid-clear (the web bug, fixed). "Empty trash" in the Deleted header — bulk permanent delete, the one action with a confirmation dialog (irreversible, no undo possible). Batch mutations in use-tasks.ts (`useBulkSoftDeleteTasks`/`useBulkRestoreTasks`/`useBulkPermanentlyDeleteTasks`), all optimistic. When the section is expanded, batch actions run a **cascade**: visible cards slide off swipe-style (30ms stagger, capped at 8) via SwipeableRow's programmatic `exit` prop, THEN the mutation fires. **Pending: on-phone review.**
- **UI slice 8 built (2026-07-10): calendar.** New Calendar tab (`app/(tabs)/calendar.tsx`): month view default (Sunday-start grid from tested `lib/tasks/calendar.ts` — buildMonthMatrix/tasksByDay/date keys), day cells show up to 3 status-colored dots (+ overflow marker), today = accent circle, days with overdue tasks tinted; tap the title → year view (12 month blocks with task counts, current month outlined); chevrons navigate month/year. Tap a day → `app/day/[date].tsx`, a regular pushed screen (native back) listing that day's tasks as swipeable cards. Recurring dailies deliberately absent from the calendar. CSV events later with the import feature. Tab bar: Tasks / Daily / Calendar / Debug. NOTE: adding routes requires regenerating typed routes (start the dev server briefly) or tsc fails on router.push paths. **Pending: on-phone review.**
- **UI slice 9 built (2026-07-10): Settings tab.** Developer decisions: password change = reset EMAIL only (completing the link needs deep links → dev-build stage; flagged); display-name/email changes require re-entering the password (re-auth via signInWithPassword); timezone is always the phone's (display-only row — all date math is already device-local wall-clock); profile pictures via `expo-image-picker` → Supabase Storage `avatars` bucket (**requires running `supabase/06-avatars-bucket.sql`**, Dashboard fallback documented in the file) → public URL in auth user metadata. Display name, avatar_url, and `urgent_threshold_hours` all live in **auth user metadata** (no settings table); `hooks/use-urgency-threshold.ts` feeds TaskCard + calendar dots (12/24/48/72h chips). Prompts use iOS-only Alert.prompt for now (Android pass later). Sign-out lives in Settings (Debug keeps its copy). Tab bar: Tasks / Daily / Calendar / Settings / Debug.
- **FUTURE (end-phase, developer idea 2026-07-10): style packs / "skins".** Swappable visual styles (card art, complete/delete/undo animations, status visuals, fonts — any card visual), authored by the developer only at first; possible paid user marketplace later; picker hidden deep in Settings to protect productivity. DO NOT build until absolutely all functionality is done — but PROTECT the skin-ready architecture: all visuals must keep flowing through `lib/theme/tokens.ts`/`use-theme.ts`, the single TaskCard, the single swipe engine (`swipeable-row.tsx`), and pill-colors. Never hard-code colors/fonts/durations in screens.
- **UI slice 10 built (2026-07-10): search + filter.** Pull down a little on the Tasks list to reveal a hidden search bar (iOS Mail pattern — `contentOffset` initial scroll; Android just shows it); "Filter" slides open urgency/category/subject chips (`components/search-filter-bar.tsx`, shared `components/collapsible-reveal.tsx` extracted from the form). Any active criterion switches the list to a flat "Results (n)" section hiding everything else; trash excluded, filters AND-combine, urgency respects the user threshold. Pure logic in `lib/tasks/filter.ts` (+6 tests, 32 total). LESSON: never round-trip source files through PowerShell Get-Content/Set-Content — PS 5.1 mangles UTF-8 (mojibake); use the Edit tool.
- **Slice 11 built (2026-07-10): notifications.** LOCAL scheduled notifications only (no server push) — verified against SDK 54 docs that these work in Expo Go, so **no dev build needed yet** (that decision defers to Stage 3 offline sync). `lib/notifications.ts` (init handler + Android channel; cancel-and-reschedule reconcile, capped at 48 soonest for iOS's 64 limit; two per task: "Task now urgent" at due−threshold, "Due in X" at due−lead); `hooks/use-notification-sync.ts` (debounced, runs in the tabs layout = signed-in only; permission requested once on first run); `hooks/use-notification-lead.ts` (`notification_lead_minutes` metadata, default 60, Settings chips 30m/1h/2h); Settings "Notifications" section shows permission state + enable button. **Verified on-phone 2026-07-10: the 1-hour pre-deadline reminder fired correctly in Expo Go.** Settings has a test-notification button + scheduled-count readout for future diagnostics.
- **Decision (2026-07-10): widgets stay on schedule (last, as the capstone)** — developer asked about lock-screen widgets; after laying out the cost (Swift/WidgetKit, forces dev build, ~15-min cloud-build iteration from Windows), they chose the roadmap order. **Developer does NOT yet have an Apple Developer account ($99/yr)** — required for ANY dev build on a real iPhone (offline sync stage needs it too, not just widgets). Enrollment takes a day or two; remind them to enroll BEFORE starting Stage 3.
- **HIG audit pass (2026-07-10), items 1–7 applied** (from the audit against docs/design/07 + 00–06): (1) undo toasts announced to VoiceOver; (2) notification taps deep-link to the task edit sheet (`hooks/use-notification-navigation.ts`, warm + cold start); (3) app badge = overdue count (set in notification sync); (4) textTertiary darkened/lightened for 4.5:1 at caption sizes (tokens + 03 doc updated); (5) Dynamic Type pass — fixed heights → minHeight on inputs/rows/chips/pills app-wide, `maxFontSizeMultiplier={1.4}` clamps on the calendar's fixed-geometry grid; (6) form-sheet grabber is honest — drag down to dismiss (follows finger, springs back under threshold) and BOTH swipe & backdrop-tap now show a "Discard changes?" confirm when fields are dirty; (7) day page: drag down from the header to dismiss (restores a standard escape after zoom replaced back-swipe). Items 6–7 are ON TRIAL — developer will report if any feel wrong. Item 8 (Liquid Glass native chrome) parked for the dev-build stage.
- **Slice 12 built (2026-07-10): CSV event import.** **Requires running `supabase/07-events.sql`** (event table: wall-clock start/end, all_day, location/notes/source, RLS, no update policy — events import+delete only, never edit, by design). `lib/events/csv.ts` (+7 tests, 40 total): forgiving parser — fuzzy column aliases (title/event/name…, date, start time…), ISO + US month-first dates, 24h + AM/PM times, timeless rows → all-day, per-row errors collected while good rows import. `lib/events/use-events.ts` (chunked bulk insert tagged with source filename; delete-all; eventsByDay). Import sheet = `app/import-events.tsx` (transparentModal; pick CSV via expo-document-picker → preview count/errors/first rows → import; delete-all with confirm), reached from a tray icon in the Calendar top bar. Events render visually distinct per docs/design/02: `components/event-card.tsx` (dashed event-blue border — RN can't do per-side dotted borders; mono time range, location caption, NOT swipeable/editable), hollow blue ring on calendar day cells vs solid task dots, "Schedule" sections on the day page and the Daily view (the uploaded-daily-schedule MUST). **07 run + import verified on-phone with a real CSV (2026-07-10).** Tap any event card → `app/event/[id].tsx` (transparentModal detail sheet: full date, mono time range, location, NOTES — the field cards don't show — source filename, and single-event delete with confirm). Events stay read-only by design. **Event colors (2026-07-10, requires `supabase/08-event-color.sql`):** optional `color` CSV column (hex or names — red/orange/yellow/green/teal/blue/indigo/purple/pink/gray, `NAMED_EVENT_COLORS` in csv.ts) + a default-color swatch row in the import sheet for rows without one (null = theme event-blue, adapts to dark mode); color drives card/detail dashed borders, time text, and the calendar day ring (first event's color). `app/import-help.tsx` = "How do I make a CSV?" sheet with a copyable AI prompt (expo-clipboard) that generates correctly-formatted CSVs.
- **STAGE 3 IN PROGRESS (2026-07-11). Decision: PowerSync.** The FULL client integration is CODED and pushed, dual-mode by design:
  - **How dual-mode works:** `lib/sync/system.ts` is the only sync file safely importable anywhere; PowerSync/op-sqlite/schema/connector load via dynamic `import()` inside `initSync()`, which only runs when NOT in Expo Go (`Constants.appOwnership === 'expo'`) AND `EXPO_PUBLIC_POWERSYNC_URL` is set in `.env`. Every data hook (`use-tasks`, `use-recurring`, `use-events`) branches on `syncDb()` per operation: local SQLite reads/writes in sync mode, the original Supabase REST path otherwise. `components/sync-bridge.tsx` (mounted in root layout) boots sync and watches the 4 tables → invalidates TanStack keys when synced changes arrive. **Expo Go therefore still works, unchanged, today.**
  - Files: `lib/sync/schema.ts` (client tables mirror Postgres; PKs become text `id`), `lib/sync/ids.ts` (offline inserts mint bigint-safe numeric ids ≈ Date.now()*1000+rand — far above the Hibernate sequence, below MAX_SAFE_INTEGER; uuid for recurring_completion), `lib/sync/connector.ts` (fetchCredentials = Supabase JWT + env endpoint; uploadData replays CRUD via supabase-js so **RLS enforces writes**; permanent errors (SQLSTATE 23*/42*) drop the op instead of retrying forever), `supabase/09-powersync-prep.sql`, `powersync/sync-rules.yaml`.
  - EAS ready: `eas.json` (development profile), bundle id `com.abuljean.multitask`, expo-dev-client installed, eas-cli dev dep, project linked (id in app.json, owner abuljean).
  - **SERVER SIDE COMPLETE (2026-07-11): SQL 08+09 run; PowerSync instance live** (URL in `.env`: `EXPO_PUBLIC_POWERSYNC_URL=https://6a5235a7367771958b46c88c.powersync.journeyapps.com`), connected to Supabase (direct connection), **Supabase client auth enabled**, sync rules deployed (with `task_id as id` alias — PowerSync requires an `id` output column). The generated SDK code on their dashboard matches our integration; ours additionally maps per-table PK columns for upload (their template's `.eq('id', …)` would break on task).
  - **iOS build still BLOCKED ON Apple identity verification (up to 48h from 2026-07-11).**
  - **WEB BUILD PROVEN (2026-07-11):** `npx expo export --platform web` succeeds (SPA, `web.output: "single"` — static prerender crashed on `window` in Node, pointless for an auth-gated app anyway) and BOOTS in a browser (sign-in renders, zero console errors; served from `dist/` via `npx serve`). Enablers: `lib/sync/system.web.ts` (web stub keeps native PowerSync out of the web bundle — Metro auto-resolves `.web.ts`; the desktop/web phase replaces it with the PowerSync WEB SDK behind the same exports) and `Platform.OS === 'web'` guards in `lib/notifications.ts`. Web stays online-mode until the desktop/web phase (docs/design/08). NOT yet done on web: real design pass (sign-in is the bare placeholder, though now themed/legible in dark mode). **Desktop layout pass (2026-07-11, developer feedback on the deployed site — pulls forward parts of docs/design/08):** **LEFT-side** nav rail on web ≥1024px (`tabBarPosition:'left'` + `tabBarVariant:'material'`, RN7 requires that pairing; bottom bar below the breakpoint; developer moved it from right → left after seeing it live — update docs/design/08's "right-side menu" when that phase starts); **whole-card hover feedback** (1.5% scale, 150ms, in swipeable-row.web.tsx — the edge zones alone were undiscoverable); **Debug tab REMOVED** (explore.tsx deleted; its jobs live in Settings/real UI now); **round 2 (2026-07-11, same session):** test-notification button + scheduled-count readout removed from Settings (diagnostics no longer needed — developer); **WEB NOTIFICATIONS live** (`lib/notifications.web.ts` — browser Notification API + in-page timers while the app is open, same urgent/due-soon triggers and reconcile as native, `setAppBadge` where supported; true closed-tab push = future server feature; Tauri desktop inherits this); day page on wide web: centered column, inline task DESCRIPTIONS (TaskCard `showDescription` prop), and clicking the blank gutters dismisses back to the calendar; Tasks tab on wide web: search bar + filter panel PERMANENTLY open (magnifier hidden; phone reveal behavior untouched); **web exit-animation bug fixed** — swipeable-row.web `commit()` now defers the mutation 250ms so the slide-off actually plays (it fired instantly before, unmounting the row mid-frame). All verified in-browser (desktop 1440 dark): exit animation samples showed full off-screen travel; day-page gutter click returned to /calendar; descriptions rendered. **Round 3 (2026-07-11):** hover aura — a status-colored glow ring (1.5pt border + soft boxShadow, fades with hover, hides once the row slides toward an action) on all swipeable rows via `hoverAuraColor`/`hoverAuraRadius` props on both swipe engines (task cards pass their status accent, recurring rows pass theme accent + pill radius); event cards show imported NOTES inline on the wide day page (`showNotes` prop); the blank area BELOW the last card on the day page also exits (flexGrow filler Pressable, wide only). Verified: urgent task hovers orange, notes render, bottom-click returns to /calendar. NOTE: Expo's dev-only `#error-overlay` root can swallow real clicks in the dev browser after an error — restart/ignore; it does not exist in production exports. **Round 4 (2026-07-11, developer reported both still broken on their machine — real root causes):** (1) exit slide was overridden by the hover-reset effect on EVERY real click (a human always hovers the zone they click; the programmatic verification click didn't hover first, so it falsely passed) — fixed with a `committing` ref guard; (2) RNW Pressable `onHoverIn` never fires on a pressable CONTAINING other pressables, so the aura/scale were dead on real mice (synthetic pointerenter on the node bypassed it) — fixed with raw `onMouseEnter`/`onMouseLeave` DOM props (`mouseHover()` helper). LESSON (also in memory): verify web interactions with the full real-pointer sequence, hover before click. **Round 5 (2026-07-11):** aura softened to a light fade (ring `${color}66`, shadow 14px `${color}2E` — developer: full-strength too heavy); filter panel starts COLLAPSED everywhere (desktop search bar stays permanently open, chips open via the Filter button); **LIGHT/DARK TOGGLE shipped on all platforms** — sun/moon button top-right on ALL FOUR TABS (shared `components/theme-toggle-button.tsx`; no Settings trip), `AppThemeProvider` in `lib/theme/use-theme.tsx` owns a per-device persisted preference (AsyncStorage `ui.themePreference`, 'system' until first toggle), `hooks/use-color-scheme(.web)` re-export the resolved scheme so navigation/status bar/browser chrome/all tokens flip together (use-theme became the provider the original comment promised; renamed .ts → .tsx). Verified in-browser: one click flips to light (#FBFBFA surface, colorScheme light, persisted), second click back to dark; 40 tests pass. **Style-pack scaffolding SHIPPED behind "coming soon" (2026-07-12, developer request):** `lib/style-packs/registry.ts` (the CURATED pack list — the only source of styles; currently just the default), provider merge in use-theme (pack colors over base palette per mode, persisted `ui.stylePack`, unknown ids fall back to default), `app/styles.tsx` sheet (pack list with applied state + working switcher, "More styles coming soon" + curation copy), low-key Styles row at the bottom of Settings. **Curation is structural: NO import UI/file picker/URL loader exists — users cannot sideload, period.** Marketplace phase adds the signed-catalog path (doc 10 APP-7, added same day: Ed25519-signed downloads verified against a baked-in public key; we review+sign+publish approved third-party packs). Shipping a new style later = add one entry to the registry. all four tab scrollables are FULL-BLEED with content centered via `pageContent` in `lib/theme/layout.ts` (maxWidth 1100, `marginHorizontal:'auto'` in contentContainerStyle — never an outer wrapper View, so the browser scrollbar sits at the window edge and swipe exits travel the whole viewport); browser chrome matches theme via `document.documentElement.style.colorScheme` (root layout — fixes the white scrollbar in dark mode); hover edge zones widened 56→120px (`swipeable-row.web.tsx`); calendar on wide web: 118pt day cells with NAMED status-tinted task bars + dashed event bars (+n more) instead of dots, full weekday names, bigger year blocks — geometry per-mode via `getGeometry(isWide)`, month FlatList key includes the mode so scroll math survives breakpoint crossings. Verified in-browser at 1440×900 dark + 375px mobile (bottom bar intact), zero console errors. **Throwaway web-test account exists: multitask.uitest.claude@gmail.com** (owns 4 seed tasks; email-confirmed directly in the DB; developer may delete it anytime).
- **FLAGGED (2026-07-11): Supabase Auth "Confirm email" is ON again** (discovered during web verification; the dev-phase decision was OFF). With the Site URL now pointing at the Render site, confirm links land on the deployed web app — workable for web signups; phone signups under Expo Go would confirm via the website then sign in on the phone. Developer should decide: keep ON (more secure) or turn OFF until the dev-build deep-link work.
- **Deployment ready (2026-07-11): `render.yaml` blueprint at repo root** — Render Static Site (free tier suffices; builds `npm ci && npx expo export --platform web`, serves `dist/`, SPA rewrite, prompts for the two `EXPO_PUBLIC_SUPABASE_*` env values). Decision: deploy to a NEW URL side-by-side; the Vaadin site keeps running (its 11 remaining users log in with old Java accounts that the new app can't authenticate). After deploying, set the new URL as Supabase Auth Site URL (password-reset emails currently point at localhost).
  - **Android dev build SUCCEEDED (2026-07-11):** compiled green on EAS first try — the full native stack (PowerSync + op-sqlite + dev client + notifications) is proven to package. APK in the Expo account (expo.dev → abuljean/multitask → builds) ready for any Android device/emulator; developer has no Android hardware currently, so on-device sync validation waits for the iOS build. NOTE: dev builds take env from the local dev server's `.env`. **DONE (2026-07-12): the three `EXPO_PUBLIC_*` vars are registered as EAS project env variables for the `preview` and `production` environments** (plaintext — they're public client values), so release builds are unblocked.
  - **STAGE 3 COMPLETE — OFFLINE SYNC VERIFIED ON-PHONE (2026-07-15).** Apple identity verified; iPhone registered (`eas device:create`); iOS dev build built, installed, and connected (notes: Apple's 1-hour security delay before install is normal; iOS Developer Mode required; hotspot networks need the Windows network profile set to Private + manual URL/QR connect — auto-discovery fails on hotspots; EAS wrote `ITSAppUsesNonExemptEncryption: false` into app.json during the build). **Developer ran the full airplane-mode test: offline create/complete/edit all worked, changes reached the website when back online, web→phone direction confirmed — "works exactly as is."** Sync-state dot recolored at developer request: BLUE filled = live (statusEventAccent), accent = actively syncing, RED OUTLINED ring + "offline" caption = offline (outlined so it can't read as an error; flag if too loud → orange fallback). NOTE: the review branch `review/coderabbit-full-repo` was MERGED to main 2026-07-16 (fast-forward) after independent verification — 94 tests green, tsc clean, web boots clean, tuned motion values + wall-clock discipline + dual-mode branching all intact.
  - **NEXT: deep OS integration phase** per the roadmap + WWDC26 shortlist above (device calendar, Quick Actions, App Intents/Siri, Spotlight, widgets incl. lock-screen, Control Center) — all now possible with the dev build. **The complete phase spec for the overnight session is `docs/OVERNIGHT-HANDOFF-os-integration.md`** (2026-07-16): branch rules, the native-module-vs-installed-build guard pattern, WP1 device calendar / WP2 quick actions + notification actions / WP3 widget scaffold / WP4 audit-deferred JS wins, verification matrix, EAS compile-check builds, morning checklist.
- **WWDC26 adoption shortlist (2026-07-12, developer criterion: ONLY user-speed/accessibility boosters; reviewed developer.apple.com/wwdc26/guides):** at the OS-integration stage → (1) **App Intents + App Shortcuts** for Siri task creation (intent schemas = natural-language actions, no fixed phrases — this IS the roadmap's voice MUST); (2) **Spotlight semantic index / App Schemas** — tasks searchable from system search, jump straight to a task; (3) **interactive/customizable widgets** (WWDC26 WidgetKit) — check off today's tasks FROM the widget; (4) **home-screen Quick Actions** (long-press icon → Quick add; old API, cheap, big win); (5) **Control Center quick-add control** — bundle with the widget Swift work. REJECTED (fail the speed test): Foundation Models/on-device AI, NowPlaying/Music, visionOS, Core Image, document apps. PARKED to polish: Liquid Glass chrome + Icon Composer layered app icon (Icon Composer is Mac-only — needs the occasional-Mac-access plan anyway).
- **OVERNIGHT AUDIT (2026-07-15/16, branch `review/coderabbit-full-repo` — local, unmerged): six parallel review agents swept the whole codebase; all safe fixes applied in 6 commits.** Full detail in `REVIEW-REPORT.md` at the repo root (read it first). Headlines: **`supabase/10-security-hardening.sql` MUST be run** (event.id GENERATED ALWAYS silently erased every sync-mode CSV import; unconfirmed-email account-linking takeover; user_id column injection; recurring-completion check-off DoS; avatars bucket caps) and **"Confirm email" is now a security invariant — keep it ON**. Client fixes: connector per-op error handling + 22\* permanent class + length caps (one bad row/too-long title used to jam or truncate sync); sign-out wipes the local sync db + query cache + notifications (cross-account leak); first-sync gate (no "empty state" flash on fresh installs); concurrent-safe optimistic rollback/settle in all three data domains; recurring temp-id swap; nav chrome now token-driven (was template teal — style-pack seam); reduced-motion honored (rule 5); TaskCard a11y label carries status+due; 44pt targets; web double-click commit guard; Expo template leftovers deleted (incl. `constants/theme.ts`, `app/modal.tsx`); `lib/supabase.web.ts` (detectSessionInUrl on web); supabase/09 re-run guards; expo-image-picker permission strings in app.json; `npm run typecheck`; tests 40→85. Deferred design decisions (urgent color-alone cue, import-sheet double primary, SheetShell extraction, chip unification, doc-hygiene list) are in the report §Deferred. CodeRabbit CLI now runs via WSL2 Ubuntu (`.coderabbit.yaml` scopes to app code; hang recovery = taskkill wsl processes).
- **Auth screens design pass (2026-07-16, branch `ui/auth-design-pass`):** sign-in/sign-up placeholders replaced with the real design — the form sits on a task-card-anatomy card (4px left accent bar, surfaceElevated, radius.card) under a "Multitask" wordmark with a JetBrains Mono dateline ("Thu, Jul 16", the due-date idiom). Shared UI in `components/auth-form.tsx` (AuthScreen/AuthField/AuthButton/AuthMessage — spec-exact primary button incl. pressed scale + loading spinner, labeled inputs with focus glow + error borders, show/hide password eye); pure logic in `lib/auth/form.ts` (validateEmail/validatePassword/friendlyAuthError mapping Supabase codes to calm copy, 16 tests). Buttons never disabled for validation — pressing surfaces field errors. Native web focus outline suppressed ONLY because the spec accent ring replaces it. Verified via headless Chrome: dark+light, 1440+390px, error states, real sign-in E2E. 110 tests total.
- **OVERNIGHT OS-INTEGRATION SESSION (2026-07-16/17, branch `overnight/os-integration` — read `docs/OVERNIGHT-REPORT-os-integration.md` first):** WP1 device-calendar sync SHIPPED (expo-calendar ~15.0.8 behind a soft-fail dynamic-import gateway `lib/device-calendar/system.ts`; pure tested planner `plan.ts` — open+dated tasks, start-of-today→90d, 30-min events, `multitask:{id}` notes marker; true-upsert reconciler `sync.ts` that only touches marked events, owns an app-created "Multitask" calendar, toggle-off deletes it; Settings toggle in auth metadata `calendar_sync_enabled`, default OFF; iOS17 full-access permission strings). WP2 SHIPPED (expo-quick-actions **6.0.0 exactly** — the SDK-54 pairing, package throws at import time without its native module so even the router wiring is dynamically imported in `lib/quick-actions/system.ts`; notification **Complete action button** via expo-notifications category — works in the CURRENT installed build). WP3 iOS widget scaffold SHIPPED (`targets/widgets/` Swift WidgetKit target via @bacons/apple-targets ^4.0.7: small/medium + lock-screen rectangular/circular, iOS17 interactive check-off AppIntent queuing ids in App Group `group.com.abuljean.multitask`, app drains queue on foreground through the optimistic mutation; JS writes a ready-to-render JSON snapshot — pure tested builder `lib/widgets/snapshot-data.ts`; `ios.appleTeamId: 942W32N3DA` set for target signing). WP4 partial: **#13 midnight rollover FIXED** (`hooks/use-today.ts` drives Tasks/Daily/calendar regrouping) and **#14 dropped-op surfacing SHIPPED** (`droppedOpCount()` via lib/sync/system, calm toast + Settings caption); SheetShell #3 and Chip #4 deliberately SKIPPED (refactors need visual verification; next session). Research residue in `docs/os-integration-notes.md` (App Intents/Siri/Spotlight/Control Center paths). 130 tests. Wifi dropped mid-session — EAS build outcomes + emulator E2E status are in the report.
- **CSV import upgrades (2026-07-21, branch `feature/csv-import-tasks` off overnight/os-integration):** (1) the copyable AI prompt (`app/import-help.tsx`) is now instruction-first — you send it BEFORE your events; it acknowledges, you paste raw events, and it ASKS for any missing REQUIRED value (title/date) instead of guessing. (2) The import sheet gains an Events / Tasks / Pick-each selector so CSV rows can import as tasks (with due dates) instead of read-only events — a row is one or the other. `lib/events/to-task.ts` (pure `eventToNewTask`: all-day→11:59 PM due, timed→exact, notes+location→description; 6 tests), `useImportTasks` batch hook (mirrors useImportEvents), `lib/events/import-labels.ts` (count/plural strings; 10 tests), `confirmDialog` gained optional `cancelLabel`. If you press Import without touching the selector, a dialog offers the tasks path BEFORE any write. 143 tests. Web-verified: AI-prompt + intro copy render, zero console errors; the file-pick→import leg is unit-tested + typechecked and needs the on-device pass. (The web file-read failure later proved to be a REAL bug — fixed 2026-07-22, see the day-view/flow entry.) MERGED to main 2026-07-23.
- **Widget upgrades + pre-merge review pass (2026-07-21/22, branches `fix/pill-delete-and-accent` → `feature/widget-events-uncomplete`):** pill delete (long-press a category/subject chip in the task form → confirm → clears it off the tasks that carry it; `useDeleteCategory`/`useDeleteSubject`); light accent swapped `#3D4A7A` → `#4954C7` (doc 03 updated); explicit Add button in the category/subject creator (Done-key-only silently lost names). Widgets: EVENTS render distinct (calendar glyph, event-blue, no check); the check button TOGGLES complete⇄un-complete (`{id,done}` queue, legacy int format tolerated); NEW `systemLarge` family (widgets can't scroll — bigger family is the answer); small-widget fallback when nothing's due today = upcoming-URGENT count → WEEK buckets ("This week"/"Next week"/"In N weeks") counting tasks AND events → all-clear check (circular never reads a bare "0 due" now). Pre-merge review of the whole 21-commit stack (self-review + CodeRabbit branch diff): fixed mixed-import duplicate-on-retry, widget snapshot upgrade-window decode tolerance (old snapshots lack events/done — LESSON: the tolerance edit itself broke the Xcode compile once, caught by the EAS build), stale `droppedCounter` after teardown, DST-unsafe 90-day calendar window. 148 tests; prod web export green. iOS builds: 6d1d85b5 green (pre-review); final green build 08e8e9d8 (commit 50a0ae3) contains ALL branch native code. MERGED to main 2026-07-23.
- **Day-view timeline + flow/security wrap-up (2026-07-22/23, tail of feature/widget-events-uncomplete):** CSV import WEB file read fixed (picker File.text(); readAsStringAsync cannot read web blobs — web import had NEVER worked; verified with the developer 284-row schedule); day page = TIMELINE (tested piecewise engine lib/tasks/day-timeline.ts: wide = two panes with events-only timeline left + normal task cards right; phone = merged full-width lane, tasks share width with colliding events; gap compression built but OFF by developer pick — gapThresholdHours: Infinity); calendar light-mode bar text auto-darkened to 7:1 (readableTextColor minRatio param), phone dot counts (blue event count when 2+, explicit +N task overflow); FLOW fixes across all transparent routes — web exits 120-130ms on timers (page under a modal is INERT until pop, so exit time = blocked input), keyed self-pop via StackActions.pop(source) (plain back() killed a quickly-opened next sheet), pointerEvents none while dismissing; split-view panes stretch + per-pane exit fillers; SECRET /marketplace storefront template (placeholder packs, Views not fake buttons, preview-honest copy, linked from nowhere); docs 10 §4.10 + 11 synced with all new customizable surfaces. Security pass (self + CodeRabbit 0.7.0) on the tail slice: file-size guard hardened (web File.size), DayTimeline props discriminated by variant, timeline overdue rows carry the warning triangle. 163 tests. **ALL FIVE BRANCHES MERGED to main 2026-07-23 (fast-forward to defb20e); Render auto-deploys the site from main.**
- **Store-prep + tour sprint (2026-08-11..15, all on main):** PowerSync outage root-caused (instance STOPPED Jul 28 in the dashboard; developer redeploy revived it — reconnect nudges shipped: foreground reconnectSync + tappable offline dot). **Interactive tour v5 SHIPPED and developer-approved on-phone**: 22 gated steps (guided task build inside the quick-add sheet incl. details/priority/category-vs-subject/notes/submit, delete->undo->complete on the tour's own task (id-tracked), Daily add+check, calendar year zoom out/in, real day visit, week+CSV, Settings); three overlay HOST instances (tabs/quick-add/day — native modals paint above root siblings) sharing context-lifted index; in-element self-drawn rings (zero scroll lag; ringPadX=0 inside the clipping form sheet); action dims are visual-only (scroll-through); keyboard-aware cards + cardPin ('when' pins top clear of the date picker); Keyboard.dismiss on step change (notes softlock); wrong-action failsafes (advanceOnAny); pathname-aware navigation (the create-freeze fix); measure-after-settle. Events emitted from real mutations incl. recurring + calendar mode switches; details-open re-announced (web starts expanded — and emits inside React state updaters get DROPPED, lesson). **Siri WORKS on-phone** (plugins/with-siri-intents.js compiles intents into the app target — pod-compiled App Shortcuts produce no metadata, Siri said "install the app"); form close() one-shot guard killed the POP warning. **Day timeline: full-day axis** (fullDay sentinels, 12AM->12AM) with 3h+ empty runs compressed to "N hr" bands, busy areas true-scale. Tab EDGE-swipe navigation + active-tab underline; quick-add autofocus deferred past sheet entrance (keyboard-first race); calendar transformOrigin NaN-proofed ("exactly 3 values" crash); tour-v4.js E2E = the 22-step benchmark. LESSON (repeated): never route \$-containing JS through bash heredocs/node -e; CRLF breaks node string replace — use the Edit tool.
- **Legal + store prep (2026-08-15):** `app/terms.tsx` (public terms — as-is warranty disclaimer, liability capped at zero, BC Canada law), **in-app account deletion** (Settings → Delete account, two confirms → `supabase/11-delete-account.sql` security-definer RPC, **run 2026-08-15**) which is Apple's hard 5.1.1(v) requirement, `app/privacy.tsx` rewritten (all processors named, public avatar bucket disclosed, explicit no-AI-processing, backup-retention nuance), and `app/support.tsx` (the required public Support URL). All three policy pages are PUBLIC routes outside both auth guards and are verified signed-out. House style for policy copy: no semicolons, no em dashes (machine-scanned).
- **iPad decision (2026-08-15): v1 is iPhone-only.** The iPad layout work is MERGED and inert — `hooks/use-wide-layout.ts` shares the desktop-web gate with tablets (two-pane day timeline, named-bar calendar, always-open search, 560pt centred sheets via `tabletSheet`), gated on live WIDTH not device because iPadOS 26 makes every app resizable. `ios.supportsTablet` is **false** only because App Store Connect demands 13-inch iPad screenshots and the developer has no iPad to verify on or shoot with. **v1.1 iPad = flip that one flag + shoot screenshots.** iPad landscape is already declared (`UISupportedInterfaceOrientations~ipad`), iPhone stays portrait.
- **iOS version reach (2026-08-15): iOS 15.1, the floor of Expo SDK 54 / RN 0.81** — verified against every pod in the stack (all sit on `min_ios_version_supported`), now pinned explicitly via `expo-build-properties` so a future dep bump fails loudly instead of silently dropping old iPhones. Widgets (17.0 target) and Siri intents (`@available(iOS 16)`) go absent on older systems rather than breaking the app. SDK 56 raises the floor to 16.4 — that's a reach regression to weigh at upgrade time. EAS already builds SDK 54 with Xcode 26, so Apple's April 2026 minimum-SDK rule is satisfied.
- **NEXT (store path): `docs/store-submission.md` is the paste-ready App Store Connect pack** — listing copy (counts checked), exact App Privacy nutrition-label answers (crash data declared NOT linked: telemetry deletes `event.user`), review notes, demo account (`multitask.uitest.claude@gmail.com` — keep alive until approved), build/submit commands, review-risk table. Remaining: developer creates the ASC record, then production build → `eas submit` → TestFlight days → submit for review.
- **Release decisions (2026-07-26):** iOS App Store FIRST; **Google Play deferred to v2** (no Play account/feature graphic/data-safety work now — the Android APK still builds for the developer's own emulator/testing). Release comes after Phase 4 (multiplatform practical phases); style packs + marketplace stay post-release.
- **After Stage 3:** deep OS integration (device calendar, widgets incl. lock-screen, Siri) → **multiplatform per docs/design/08** (iPad → Android polish+widgets → desktop via Tauri → web replaces Vaadin → Apple Watch) → polish/ship (Stage 5) → style packs + marketplace per docs/design/09 (very end).

> **PHASING — read this, it prevents scope creep.** This project is built in distinct phases, NOT all at once:
> 1. **NOW: build the native app (this repo)** completely — it is the design source of truth for the whole product.
> 2. **LATER (separate phase): rebuild the web app in React** to match the native design. The existing web app is Vaadin Flow, which is design-restrictive; it will eventually be replaced with a React web app that reuses the native app's design system, components, and logic. This is intended and already planned — BUT it is a *later* phase.
> - **Do NOT attempt the web rebuild during the native build.** Doing both at once doubles scope and risks the project never shipping. The current Vaadin web app keeps running as-is (it works, it's deployed, it's the backend reference) until the native app is done. Only then does the web get its React redesign.

> **All knowledge/context files are ALREADY IN THIS PROJECT on disk — read them directly, they are not uploaded to you separately.** This `CLAUDE.md` is at the repo root. The full briefing and roadmap are in `docs/`. The design system is in `docs/design/`. The existing web app's source (data model + visual DNA to carry forward, NOT code to reuse) is in `reference/`. Open and read these files from the project as needed rather than asking the developer to paste them.

> **Keep this file current:** at the end of any meaningful work session, update this "Current state" section (and any decisions made) so the next session starts accurate. When a design or architecture decision is resolved, record it in the relevant file (`docs/design/*` for design, this file or the handoff for project decisions) so the choice persists across sessions.

## Existing web app (shared backend context)
- Stack: Vaadin 25 + Spring Boot 4 + Java 24, Supabase Postgres, deployed on Render (https://taskmanager-gcvv.onrender.com).
- Data model: tasks have title, description, due date (wall-clock LocalDateTime), category, subject, priority (Integer rank; null=none), completion. Users have email, password, enabled, urgency-threshold-hours, timezone.
- **Critical Supabase note:** must use the **transaction-mode pooler (port 6543)**, not session mode — session mode caps at 15 clients and fails with EMAXCONNSESSION.
- **Existing web visual DNA to carry forward:** status-driven card backgrounds (default white, ongoing green `#f0fdf4`, urgent orange `#fff7ed`, overdue red `#fef2f2`), colored category/subject pills, priority tier badges (1st/2nd/3rd), floating action button on each card, muted secondary text (`#666`/`#888`). This is the DNA to *elevate* on native, not throw away — the app should feel like the same product, refined.

## Key feature requirements for the native app (summary — full detail in the handoff doc)
- **Must-haves:** login on all platforms (Supabase Auth + RLS); notifications when a task becomes urgent and ~1–2h before deadline; dark mode + themes; tasks pushed to the phone's native calendar; CSV calendar import creating *events* (not tasks), visually distinct from tasks; a "Today" view with daily-refreshing recurring tasks (NOT shown on the calendar); home-screen AND lock-screen widgets showing today's tasks (overdue first, else next task by date); cross-device sync via one account; PC launch-on-startup.
- **Calendar view:** year → month (day boxes) → day drill-down.
- **Mobile usability (top priority):** fast quick-add requiring only **title + time** (other fields optional, out of the way); **swipe-to-complete** and **swipe-to-delete** with good animations; bulk "clear completed"; fast app open landing on the task/Today view; **calendar picker for date, dropdown for time**.
- **Always show** task title + due date; category/subject/priority pills can be small but present.
- **Voice (ambitious, scope realistically):** Siri/Assistant create/edit/delete/complete tasks. Start with "create a task by voice."
- **Animations:** completion/deletion animated; builder has design freedom, must work on mobile AND PC, must not be distracting (it's a task manager first).
- **Open to builder's judgment:** color palette, typography, layout, animation style, iconography, overall look, screen/nav design. Fill in missing crucial features sensibly and flag them.
- **Known web bug to fix (when web is revisited):** deleting a task collapses the completed group, making bulk-clearing tedious — add bulk delete / avoid the collapse.

---

## Design principles — READ BEFORE WRITING ANY UI CODE

These principles override defaults. The single biggest risk for this build is that it comes out looking like a generic AI-generated task app (rounded rectangles, purple/blue gradient hero, Inter everywhere, three-color palette with no opinions). Do not let that happen. When making a UI decision, consult the files in `docs/design/` — they exist to give you opinions on the things that would otherwise default to generic.

### The three-word design brief
**Calm, specific, tactile.** *Calm* because it's a task manager first — visuals must never fight the content. *Specific* because generic is the failure mode — every element should feel chosen, not defaulted. *Tactile* because native lets us make things feel physical (swipe, snap, spring, weight) in a way the web version can't.

### Non-negotiable rules
1. **The status colors are sacred.** Ongoing/urgent/overdue are the visual heartbeat of this app. On any surface, in any theme, the four states (default, ongoing, urgent, overdue) must be instantly distinguishable and never conveyed by color alone (add subtle icon/border/weight cues). Exact tokens live in `docs/design/02-components.md`.
2. **Title + due date are always visible and legible at glance distance.** Everything else on a card is subordinate. If you're about to shrink the title to fit a badge, shrink the badge instead.
3. **One primary action per screen.** The quick-add FAB on the task list. The save button in a form. Never two filled buttons competing.
4. **Motion has a job.** No animation exists for decoration. Every transition either orients (came from / going to), gives feedback (tap, swipe, complete), or expresses continuity. See `docs/design/05-motion.md`.
5. **Respect `prefers-reduced-motion`.** Motion is required to gracefully degrade — this is an accessibility hard-line, not a nice-to-have. The React Native equivalent is `AccessibilityInfo.isReduceMotionEnabled()`.
6. **Platform conventions win over cross-platform sameness.** iOS gets iOS back-swipes and large titles; Android gets Material navigation and system back. See `docs/design/01-platforms.md` for the conflict-resolution rules.
7. **Density is a feature, not a bug.** This is a power tool for someone who uses it daily. Don't add whitespace to look "clean" if it makes the user scroll more to see their tasks. Follow the density guidance in `docs/design/03-layout-type-color.md`.

### The anti-generic checklist
Before shipping any screen, mentally check `docs/design/06-anti-generic.md`. The most common tells: purple/indigo hero gradients, Inter as the only font, all-rounded corners at the same radius, empty states with a giant illustration and a single CTA, filled primary + outline secondary buttons on every screen with no thought to hierarchy, drop shadows on everything, emoji as icons, "AI-suggested" copywriting ("Let's get started!", "You've got this!"). Don't do these.

---

## Product success rubric (what "working" means — added 2026-07-25)
The product is succeeding when ALL of these hold. Check them when judging any release; a real-world failure of one becomes a test case.
1. **The developer lives in it:** ≥10 tasks completed per week through the app (not the old site), sustained.
2. **Zero data loss, ever:** no task/event created on any device fails to reach every other signed-in surface within 60s of connectivity. (Guarded by: PowerSync + dropped-op surfacing + nightly DB backups.)
3. **Cold open → today's tasks in under 2 seconds** on the phone; the answer to "what's due today?" never requires a second interaction.
4. **Quick-add ≤ 10 seconds** title-to-saved for a titled, timed task.
5. **A migrated web user reaches their old tasks unaided:** sign up with the same email → confirm → tasks appear. No support step.
6. **Offline is invisible:** airplane-mode create/complete/edit behaves identically to online; the only difference is the status dot.
7. **Notifications fire within a minute** of their scheduled time; the badge count is never wrong on app open.
8. **No silent failures in production:** every crash/error surfaces in telemetry (once wired) and each unique one gets a test or a fix.
9. **Design holds its own bar:** every shipped screen passes docs/design/06's checklist and rule 1 (status never by color alone).
10. **The suite stays green:** no commit lands with failing tests; the benchmark only grows.

## Operating principles — agentic work (Manager's Playbook Part 8, adopted 2026-07-25)
```
PLANNING
1. Before building, produce an org-design document containing: goal (a domain
   outcome, not "use AI"), roles, inputs/outputs per role, pipeline order,
   success rubric, roadmap with a small first milestone.
2. Think as the manager, not the drone. If instructions are ambiguous, clarify
   the spec before executing.

DECOMPOSITION
3. One agent, one job. A role's job must be statable in one sentence without
   the word "and". If it cannot, split the role.
4. Scope each agent narrower than a human job description.
5. Every step emits a visible artifact. Every handoff is verified. No
   invisible steps; never skip steps that appear optional.

MEMORY & GROUNDING
6. For each agent define three buckets: always-knows / receives-this-run /
   passes-forward. Exclude all other context.
7. Assert nothing without a source. Prefer retrieval over recall. Flag
   uncertainty explicitly instead of guessing.

RULES VS JUDGMENT
8. Enforce mechanically checkable properties (formats, schemas, thresholds,
   citation presence) with code, never with model judgment.
9. Reserve model judgment for relevance, quality, synthesis, and tone —
   graded against a written rubric with examples.

EVALUATION
10. Maintain a fixed benchmark of test cases with known-good answers.
    Run it after every change. No change ships on a red or untested benchmark.
11. Improvement is a number, not a feeling.

AUDIT LOOP (ON ANY FAILURE)
12. Read the full transcript. Locate the first divergence from correct.
13. Classify the root cause as exactly one of: INSTRUCTION (spec ambiguous),
    DECOMPOSITION (task too big), INFORMATION (missing fact/source).
14. Make one fix matching the class. Re-run the full benchmark. Commit if
    improved without regression; otherwise revert and reclassify.

SCALE
15. Start with one real task currently done by hand in 30+ minutes. Expand
    one niche at a time, only after the benchmark is green.
16. Version everything — prompts, rubrics, evals, outputs — in Git.
17. Validate on history (backtest), then deploy small (forward test), and
    feed real-world failures back into the benchmark.
```

## Working style
- **CHANGELOG DISCIPLINE (standing instruction 2026-08-26): every commit to main with USER-VISIBLE change also updates `lib/updates/entries.ts`** (the website's Updates tab) — same commit or immediately after. Skip pure-internal commits. Plain language, no semicolons, no em dashes, newest first; group a burst of same-day changes into one entry.
- Developer is learning React Native as they go — explain reasoning, go step by step, one piece at a time (describe → build → test → iterate).
- **Do NOT enable "accept all" edits blindly early on** — approve changes individually until trust is built.
- **Commit often** (each commit is a restore point). Secrets (Supabase keys, credentials) NEVER go in the repo — use env files excluded via .gitignore.
- Verify bleeding-edge API/library details against current docs rather than assuming; the ecosystem moves fast.
- **When a design decision comes up that isn't covered by the files in `docs/design/`**, propose an option grounded in the principles above and flag it to the developer rather than silently picking a default. Add the resolved decision back to the relevant file so the choice persists.

## Full reference (all files are in this project — read from disk)
- **Project briefing & roadmap:** `docs/TaskApp-Master-Handoff.md` and `docs/TaskApp-Native-Roadmap.md` — open these for full product/technical detail.
- **Existing web app source (reference only):** `reference/web-app-source/` (Java + offline.html) and `reference/config-and-infra/` (pom.xml, application.properties with the Supabase pooler fix, security config, repos). This is the data model, logic, and visual DNA to carry forward — NOT code to reuse directly.
- **Design system:** `docs/design/` folder:
  - `00-design-handoff.md` — the "why" behind every choice; read once, refer back to reconcile disagreements.
  - `01-platforms.md` — iOS / Android / desktop / web conventions and how to resolve conflicts.
  - `02-components.md` — concrete specs for buttons, cards, forms, pills, states.
  - `03-layout-type-color.md` — spacing/type/color scales and dark-mode discipline.
  - `04-gestures-and-interaction.md` — swipe, drag, quick-add, keyboard on desktop.
  - `05-motion.md` — timing, easing, the transitions catalog.
  - `06-anti-generic.md` — the "don't ship it looking like every AI-generated app" checklist.
  - `07-apple-hig-reference.md` — comprehensive Apple HIG reference (added by the developer 2026-07-10, covers all Apple platforms incl. the Liquid Glass era). Use it alongside 00–06 for design audits, and it becomes central again at the multiplatform/sync phase (iPad/macOS/watch conventions live there). Where it conflicts with 01-platforms, they mostly agree; developer decisions recorded in 02–05 win over both.
  - `08-multiplatform-plan.md` — **the platform expansion spec (developer decisions 2026-07-11):** iPad (scaled, two-pane wide layouts), Android (same app + Kotlin/Glance widgets), Apple Watch (Swift + PowerSync Swift SDK; tap = complete, long-press = delete, Crown scrolls, NO creation/search/month-grid), desktop Windows+macOS (web export wrapped in Tauri; task creation is a WINDOW with all fields; hover-reveal edge actions replace swipes; right-side menu; launch-on-startup), web (replaces the Vaadin site with the desktop design). Build order and cross-platform invariants inside.
  - `10-style-pack-requirements.md` — **the formal style-pack Requirements Document (2026-07-11):** numbered FMT/VAL/APP/PAY/NFR requirements, the complete customization inventory with exact defaults + allowed ranges (motion table mirrors the shipped hand-tuned values), geometry constants for designers, the locked list, marketplace backend schema (style_pack + pack_entitlement) and the WEBSITE-Stripe payment architecture with signed-URL downloads. Build style packs FROM THIS DOC. **Expanded 2026-07-11 (developer requests):** inventory ~3×'d (tab bar, calendar shapes, empty states, app icons, etc.); per-status COMPLETE card designs (six statuses incl. deleted); hover effects for all card types (web/desktop, per-status, §4.5); creator-authored animations (Lottie/sprite sheets on 11 named hooks with hard duration caps, reduced-motion skips them, §4.6) and creator sound files (§4.7) — both promoted v2 → v1; FMT-8/9 + VAL-7/8/9 added.
  - `11-style-pack-ai-designer-prompt.md` — **the copyable AI designer prompt (2026-07-11):** teaches any AI the app's full visual design (described in words, with real token hexes) + every customizable slot, ranges, locked list, geometry, and the exact output files, so it can co-design a valid `.mtstyle` pack. Ships on the marketplace designer-guide page (same pattern as the CSV import AI prompt in `app/import-help.tsx`). KEEP IN SYNC with doc 10 whenever the inventory changes.
  - `09-style-packs.md` — **the style-pack/marketplace spec (developer decisions 2026-07-11):** full customizable inventory (tokens, card art, parametric animations with ranges, per-screen items, per-device sections), the DECIDED pack format (`.mtstyle` = zip with manifest.json/tokens.json/motion.json/assets/fonts/previews — declarative only, NO code, validated on import incl. contrast + calm-test), Settings placement (low-key "Styles" near bottom; marketplace link smaller beneath it; marketplace is its own page with video/GIF previews), phased marketplace (dev packs first; user submissions + IAP/payout constraints flagged), and the designer guide content (geometry, motion ranges, submission requirements).
